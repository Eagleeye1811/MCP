# MCP Server – Inspector Guide

## 🚀 Overview
This project is a simple **Model Context Protocol (MCP)** server built using:

- `@modelcontextprotocol/sdk`
- `@modelcontextprotocol/inspector`
- `zod`

You can run the server normally or debug it interactively using the **MCP Inspector UI**.

---

## 📦 Installation

Install all dependencies:

npm install

To launch the Inspector UI:

npm run inspect